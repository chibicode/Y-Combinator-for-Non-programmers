import { ExpressionRunnerShorthandConfig } from '../expressionRunnerShorthandConfig'

const config: ExpressionRunnerShorthandConfig = {
  runner: 'simple',
  lessonExpressionsKey: 'qhnf',
  showPriorities: true,
  containerSize: 'xs',
  variableSize: 'sm'
}

export default config
