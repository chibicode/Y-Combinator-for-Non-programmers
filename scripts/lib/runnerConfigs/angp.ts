import { ExpressionRunnerShorthandConfig } from '../expressionRunnerShorthandConfig'

const config: ExpressionRunnerShorthandConfig = {
  runner: 'simple',
  lessonExpressionsKey: 'nxmy',
  showPriorities: true,
  containerSize: 'xs',
  variableSize: 'xs',
  nextIterations: 26
}

export default config
