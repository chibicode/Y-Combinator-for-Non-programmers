import { ExpressionRunnerShorthandConfig } from '../expressionRunnerShorthandConfig'

const config: ExpressionRunnerShorthandConfig = {
  runner: 'simple',
  lessonExpressionsKey: 'hctu',
  showPriorities: true,
  containerSize: 'xs',
  variableSize: 'xs',
  isDone: true
}

export default config
