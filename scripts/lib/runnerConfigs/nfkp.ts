import { ExpressionRunnerShorthandConfig } from '../expressionRunnerShorthandConfig'

const config: ExpressionRunnerShorthandConfig = {
  runner: 'playButtonOnly',
  lessonExpressionsKey: 'lles',
  showPriorities: true,
  skipToTheEnd: false,
  speed: 1.75,
  nextIterations: 16,
  variableSize: 'sm'
}

export default config
